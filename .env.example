# ============================================================================
# VARIABLES DE ENTORNO - CHATBOT DEMO
# ============================================================================
# IMPORTANTE: Copia este archivo como .env.local y configura los valores reales
# El archivo .env.local debe estar en .gitignore y NUNCA subirse al repositorio

# ============================================================================
# CONFIGURACIÓN DEL BACKEND PHP
# ============================================================================

# API Key de Google Gemini (OBLIGATORIO)
# Obtener en: https://aistudio.google.com/app/apikey
GEMINI_API_KEY=gemini_api_key_here

# ============================================================================
# CONFIGURACIÓN DEL FRONTEND (VITE) - SIMPLIFICADA
# ============================================================================

# URL base de la API - Vite automáticamente seleccionará desarrollo vs producción
# En desarrollo (npm run dev): usa este valor
# En producción (npm run build): usa este valor, pero puedes sobrescribirlo con archivos específicos
VITE_API_BASE_URL=http://localhost:8080

# Timeout de las peticiones API (en milisegundos)
VITE_API_TIMEOUT=30000

# ============================================================================
# CONFIGURACIÓN POR ENTORNO ESPECÍFICO
# ============================================================================
# Para diferentes entornos, puedes crear archivos específicos:
# 
# .env.local          -> Desarrollo local (ignorado por git)
# .env.development    -> Configuración de desarrollo 
# .env.staging        -> Configuración de staging
# .env.production     -> Configuración de producción
#
# Vite carga automáticamente el archivo correcto según el modo:
# - npm run dev -> carga .env.development y .env.local
# - npm run build -> carga .env.production
#
# IMPORTANTE: Solo usa VITE_API_BASE_URL (sin sufijos DEV/PROD)
# El entorno de Vite (import.meta.env.DEV/PROD) determina automáticamente el modo

# ============================================================================
# CONFIGURACIÓN OPCIONAL
# ============================================================================

# Habilitar logs de debug (true/false)
DEBUG_MODE=false