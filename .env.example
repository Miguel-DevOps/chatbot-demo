# ============================================================================
# ENVIRONMENT VARIABLES - CHATBOT DEMO
# ============================================================================
# IMPORTANT: Copy this file as .env.local and configure the real values
# The .env.local file should be in .gitignore and NEVER committed to repository

# ============================================================================
# PHP BACKEND CONFIGURATION
# ============================================================================

# Google Gemini API Key (REQUIRED)
# Get it from: https://aistudio.google.com/app/apikey
GEMINI_API_KEY=gemini_api_key_here

# Application Environment (development, testing, production)
APP_ENV=development

# Logging Level (debug, info, warning, error)
LOG_LEVEL=debug

# Redis Configuration
REDIS_HOST=redis
REDIS_PORT=6379

# ============================================================================
# OPENTELEMETRY OBSERVABILITY (OPTIONAL)
# ============================================================================

# Enable/disable distributed tracing (true/false)
OTEL_TRACES_ENABLED=false

# Service identification for telemetry
OTEL_SERVICE_NAME=chatbot-demo-api
OTEL_SERVICE_VERSION=1.0.0

# OpenTelemetry collector endpoint
OTEL_EXPORTER_OTLP_ENDPOINT=http://otel-collector:4318

# ============================================================================
# FRONTEND CONFIGURATION (VITE)
# ============================================================================

# API Base URL - Vite automatically selects development vs production
# In development (npm run dev): uses this value
# In production (npm run build): uses this value, but can be overridden with specific files
VITE_API_BASE_URL=http://localhost

# API request timeout (in milliseconds)
VITE_API_TIMEOUT=30000

# ============================================================================
# ENVIRONMENT-SPECIFIC CONFIGURATION
# ============================================================================
# For different environments, you can create specific files:
# 
# .env.local          -> Local development (ignored by git)
# .env.development    -> Development configuration 
# .env.staging        -> Staging configuration
# .env.production     -> Production configuration
#
# Vite automatically loads the correct file based on mode:
# - npm run dev -> loads .env.development and .env.local
# - npm run build -> loads .env.production
#
# IMPORTANT: Only use VITE_API_BASE_URL (without DEV/PROD suffixes)
# Vite environment (import.meta.env.DEV/PROD) automatically determines the mode

# ============================================================================
# TESTING CONFIGURATION (OPTIONAL)
# ============================================================================

# Database path for testing (PHPUnit)
DB_PATH=tests/data/test.db

# Knowledge base path for testing
KNOWLEDGE_PATH=tests/data/knowledge

# ============================================================================
# OPTIONAL CONFIGURATION
# ============================================================================

# Enable debug logs (true/false)
DEBUG_MODE=false