name: CI — Security & Build

on: workflow_dispatch

jobs:
  security-and-build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js (with pnpm cache)
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'pnpm'

      - name: Enable pnpm (Corepack)
        run: corepack enable

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Run security audit
        run: |
          # Simple audit: fail job if audit reports issues
          pnpm audit || { echo "❌ Security audit reported issues"; exit 1; }

      - name: Run linter (if defined)
        run: |
          if pnpm -s run | grep -q "lint"; then
            pnpm lint
          else
            echo "⚠️ No lint script defined, skipping lint step"
          fi

      - name: Build project (if defined)
        run: |
          if pnpm -s run | grep -q "build"; then
            pnpm build
          else
            echo "⚠️ No build script defined, skipping build step"
          fi

      - name: Security scan summary
        run: |
          echo "🔒 Security & Build job complete"
          echo "✅ If this job passed, basic CI checks succeeded"
